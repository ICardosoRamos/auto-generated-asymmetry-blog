PORT=4000

# Database
DB_HOST=db
DB_PORT=5432
DB_USER=blog_user
DB_PASSWORD=blog_password
DB_NAME=blog_db

# Auth for admin-only generation endpoint
ADMIN_SECRET=changeme

# Cron expression for daily article generation (defaults to 9am UTC)
ARTICLE_CRON_EXPRESSION=0 9 * * *

# AI provider configuration (OpenAI)
AI_PROVIDER=openai
OPENAI_API_KEY=sk-xxxxx
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_BASE_URL=https://api.openai.com/v1
AI_MAX_TOKENS=700
AI_TIMEOUT_MS=15000
